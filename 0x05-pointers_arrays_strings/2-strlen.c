#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: The parameter
 *
 * Author: @gadcode
 * Date: 19/09/2023
 *
 * Return: Length of the string
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)

	return (i);
}
